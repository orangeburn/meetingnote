Place Windows ffmpeg binaries here to eliminate console-window flashes during chunking.

Expected files:
- ffmpeg.exe
- ffprobe.exe

Notes:
- Use official static builds if possible (real .exe, not a shim/wrapper).
- After adding the binaries, restart the Electron app / ASR server.
